pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    // Commands to build your Docker image
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    // Commands to deploy your Docker image
                }
            }
        }
    }
}
